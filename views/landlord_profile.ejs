<!DOCTYPE html>
<html lang="en">
  <head>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css"
    />

    <script
      src="https://code.jquery.com/jquery-3.7.1.min.js"
      integrity="sha256-/JqT3SQfawRcv/BIHPThkBvs0OEvtFFmqPF/lYI/Cxo="
      crossorigin="anonymous"
    ></script>
    <style>
      /* Reset default margin and padding */
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: Arial, sans-serif;
      }

      /* Body styles */
      body {
        font-size: 16px;
        line-height: 1.6;
        background-color: #f7f7f7;
        color: #333;
        background-image: url("/images/b2.jpg");
        display: flex;
      }

      /* Sidebar styles */
      .sidebar {
        background-color: #00022e;
        color: #fff;
        padding: 1rem;
        width: 20vw;
        height: 100vh;
        flex: 0 0 auto;
        position: fixed;
        top: 0;
        left: 0;
        overflow-y: auto;
        transition: left 0.3s ease;
      }

      .logo {
        font-size: 0.5rem;
        margin-bottom: 1rem;
        width: 5rem;
        height: auto;
        border-radius: 40px;
      }

      .nav-links {
        list-style: none;
        margin-bottom: 1rem;
      }

      .nav-links li {
        margin-left: 0;
      }

      .nav-links a {
        display: block;
        padding: 0.5rem 1rem;
        text-decoration: none;
        color: #fff;
        position: relative;
        font-weight: bold;
      }

      .nav-links a::before {
        content: "";
        position: absolute;
        width: calc(100% - 2rem);
        height: 2px;
        bottom: -5px;
        left: 1rem;
        background-color: #fff;
        visibility: hidden;
        transform: scaleX(0);
        transition: all 0.3s ease-in-out;
      }

      .nav-links a:hover::before {
        visibility: visible;
        transform: scaleX(1);
      }

      /* Hamburger menu icon */
      .menu-icon {
        position: fixed;
        top: 1rem;
        right: 1rem;
        cursor: pointer;
        z-index: 999;
        display: none;
      }

      .menu-icon i {
        font-size: 1.5rem;
        color: #02a0f6;
      }

      /* Main content styles */
      .main-content {
        padding: 2rem;
        margin-left: 20vw;
        width: 80vw;
        min-height: 100vh;
        background-color: rgba(
          255,
          255,
          255,
          0.8
        ); /* Slightly opaque white background */
      }

      .profile-pic {
        margin-bottom: 20px;
        text-align: center;
      }

      .rounded-pic {
        width: 150px;
        height: 150px;
        border-radius: 50%;
        border: 3px solid #fff;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.3);
        object-fit: cover;
      }

      .username {
        text-align: center;
        color: black;
        font-size: xx-large;
        font-weight: bolder;
      }

      .page-title {
        color: #048e04;
        text-align: center;
        margin-bottom: 2rem;
      }

      .profile-form {
        width: 80%;
        margin: 0 auto;
        padding: 20px;
        background-color: #fff;
        border-radius: 10px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      }

      .profile-form label,
      .profile-form input[type="text"] {
        display: block;
        margin-bottom: 1rem;
      }

      .profile-form input[type="text"] {
        width: calc(100% - 20px);
        padding: 8px;
        border: 1px solid #ccc;
        border-radius: 5px;
      }
      .profile-form input[type="password"] {
        width: calc(100% - 20px);
        padding: 8px;
        border: 1px solid #ccc;
        border-radius: 5px;
      }

      .profile-form button[type="submit"] {
        padding: 10px 20px;
        background-color: green;
        color: #fff;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        display: block;
        width: 100%;
        margin-top: 1rem;
      }

      .error-message {
        background-color: #f8d7da;
        color: #b81f1f;
        padding: 8px;
        border-radius: 5px;
        margin-bottom: 1rem;
        text-align: center;
        width: 97%;
      }

      /* Media Queries */
      @media screen and (max-width: 768px) {
        .sidebar {
          left: -20vw;
        }

        .menu-icon {
          display: block;
        }

        .main-content {
          margin-left: 0;
          width: 100%;
        }
      }
    </style>
  </head>
  <body>
    <% include("./partials/progress-bar") %>

    <div class="sidebar" id="sidebar">
      <img class="logo" src="/images/logo.jpeg" alt="Logo" />
      <ul class="nav-links">
        <!-- Sidebar links -->
        <li>
          <a href="/landlord"
            ><i
              class="fa fa-dashboard"
              aria-hidden="true"
              style="color: #c68417; width: 30px; font-size: x-large"
            ></i>
            Home</a
          >
        </li>
        <li>
          <a href="/landlord/add_hostel"
            ><i
              class="fa fa-plus-square"
              aria-hidden="true"
              style="color: #0da2f1; width: 30px; font-size: x-large"
            ></i>
            Add Hostel</a
          >
        </li>
        <li>
          <a href="/landlord/delete_hostel"
            ><i
              class="fa fa-trash-o"
              aria-hidden="true"
              style="color: #eb0c0c; width: 30px; font-size: x-large"
            ></i>
            Delete Hostel</a
          >
        </li>
        <li>
          <a href="/landlord/confirm_booking_landlord"
            ><i
              class="fa fa-check-square"
              aria-hidden="true"
              style="color: #048e04; width: 30px; font-size: x-large"
            ></i>
            Approve Booking</a
          >
        </li>
        <li>
          <a href="/landlord/booking_history"
            ><i
              class="fa fa-history"
              aria-hidden="true"
              style="color: burlywood; width: 30px; font-size: x-large"
            ></i>
            Bookings History</a
          >
        </li>
        <li>
          <a href="/landlord/agent-landlord"
            ><i
              class="fa fa-address-card"
              aria-hidden="true"
              style="color: #07f9f5; width: 30px; font-size: x-large"
            ></i>
            House Agents</a
          >
        </li>
        <li>
          <a href="/landlord/landlord_profile"
            ><i
              class="fa fa-user"
              aria-hidden="true"
              style="color: gold; width: 30px; font-size: x-large"
            ></i>
            My Profile</a
          >
        </li>
        <li>
          <a href="/logout"
            ><i
              class="fa fa-sign-out"
              aria-hidden="true"
              style="color: rgb(255, 0, 93); width: 30px; font-size: x-large"
            ></i>
            Sign-Out</a
          >
        </li>
      </ul>
    </div>

    <div class="menu-icon" id="menu-icon">
      <i class="fa fa-bars"></i>
    </div>

    <div class="main-content">
      <div class="profile-pic">
        <img
          src="/images/profile.jpeg"
          alt="Profile Picture"
          class="rounded-pic"
        />
      </div>

      <div class="username">
        <% if (username) { %> Username: <%= username %> <% } %>
      </div>

      <h1 class="page-title">User Profile</h1>

      <form
        action="/landlord/landlord_profile"
        method="POST"
        class="profile-form"
      >
        <div>
          <label for="fname">First Name</label>
          <input
            type="text"
            id="fname"
            name="fname"
            value="<%= profile ? profile.fname : '' %>"
          />
          <% if (errors.find(e => e.param === 'fname')) { %>
          <div class="error-message">
            <%= errors.find(e => e.param === 'fname').msg %>
          </div>
          <% } %>
        </div>
        <div>
          <label for="lname">Last Name</label>
          <input
            type="text"
            id="lname"
            name="lname"
            value="<%= profile ? profile.lname : '' %>"
          />
          <% if (errors.find(e => e.param === 'lname')) { %>
          <div class="error-message">
            <%= errors.find(e => e.param === 'lname').msg %>
          </div>
          <% } %>
        </div>
        <div>
          <label for="phone">Phone</label>
          <input
            type="text"
            id="phone"
            name="phone"
            value="<%= profile ? profile.phone : '' %>"
          />
          <% if (errors.find(e => e.param === 'phone')) { %>
          <div class="error-message">
            <%= errors.find(e => e.param === 'phone').msg %>
          </div>
          <% } %>
        </div>
        <button type="submit">Update Profile</button>
      </form>

      <!-- Form for changing password -->
      <h2 class="page-title" style="color: black">Update Password</h2>

      <form
        id="passwordForm"
        action="/landlord/change_password"
        method="POST"
        class="profile-form"
      >
        <!-- Current Password Input -->
        <div class="form-group">
          <label for="currentPassword">Current Password</label>
          <input
            type="password"
            id="currentPassword"
            name="currentPassword"
            class="form-control"
            required
          />
          <% if (errors.find(e => e.param === 'currentPassword')) { %>
          <div class="error-message">
            <%= errors.find(e => e.param === 'currentPassword').msg %>
          </div>
          <% } %>
        </div>

        <!-- New Password Input -->
        <div class="form-group">
          <label for="newPassword">New Password</label>
          <input
            type="password"
            id="newPassword"
            name="newPassword"
            class="form-control"
            required
          />
          <% if (errors.find(e => e.param === 'newPassword')) { %>
          <div class="error-message">
            <%= errors.find(e => e.param === 'newPassword').msg %>
          </div>
          <% } %>
        </div>

        <!-- Confirm New Password Input -->
        <div class="form-group">
          <label for="confirmPassword">Confirm New Password</label>
          <input
            type="password"
            id="confirmPassword"
            name="confirmPassword"
            class="form-control"
            required
          />
          <% if (errors.find(e => e.param === 'confirmPassword')) { %>
          <div class="error-message">
            <%= errors.find(e => e.param === 'confirmPassword').msg %>
          </div>
          <% } %>
        </div>

        <!-- Submit Button -->
        <button type="submit" class="btn btn-primary">Change Password</button>
      </form>
    </div>

    <script>
      $(document).ready(function () {
        $("#menu-icon").click(function () {
          $("#sidebar").toggleClass("open");
        });
      });

      // Client-side validation for password confirmation
      $("#passwordForm").on("submit", function (event) {
        // Clear any existing error messages
        $(".error-message").remove();

        var password = $("#newPassword").val();
        var confirmPassword = $("#confirmPassword").val();

        if (password !== confirmPassword) {
          // Add the error message under the confirm password field
          $("#confirmPassword").after(
            '<div class="error-message">Passwords do not match!</div>'
          );

          event.preventDefault(); // Prevent form submission
        }
      });
    </script>
  </body>
</html>
