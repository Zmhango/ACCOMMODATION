<!DOCTYPE html>
<html lang="en">
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css"
    />
    <style>
      /* Reset default margin and padding */
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: Arial, sans-serif;
      }

      /* Body styles */
      body {
        display: flex;
        min-height: 100vh;
        background-color: #f7f7f7;
        color: #333;
        background-image: url("/images/beauty3.jpg");
        background-size: cover;
        background-attachment: fixed;
        background-repeat: no-repeat;
        overflow-x: hidden;
      }

      /* Sidebar styles */
      .sidebar {
        background-color: #00022e;
        color: #fff;
        padding: 0.1rem;
        width: 20vw;
        height: 100vh;
        flex: 0 0 auto;
        position: fixed;
        top: 0;
        left: 0;
        overflow-y: auto;
        transition: left 0.3s ease;
      }

      .logo {
        font-size: 0.5rem;
        margin-bottom: 1rem;
        width: 5rem;
        height: auto;
        border-radius: 40px;
      }

      .nav-links {
        list-style: none;
        margin-bottom: 1rem;
      }

      .nav-links li {
        margin-left: 0;
      }

      .nav-links a {
        display: block;
        padding: 0.5rem 1rem;
        text-decoration: none;
        color: #fff;
        position: relative;
        font-weight: bold;
      }

      .nav-links a::before {
        content: "";
        position: absolute;
        width: calc(100% - 2rem);
        height: 2px;
        bottom: -5px;
        left: 1rem;
        background-color: #fff;
        visibility: hidden;
        transform: scaleX(0);
        transition: all 0.3s ease-in-out;
      }

      .nav-links a:hover::before {
        visibility: visible;
        transform: scaleX(1);
      }
      /* Hamburger menu icon */
      .menu-icon {
        position: fixed;
        top: 1rem;
        right: 1rem;
        cursor: pointer;
        z-index: 999;
        display: none;
      }

      .menu-icon i {
        font-size: xx-large;
        color: #02a0f6;
      }

      main {
        flex-grow: 1;
        margin-left: 250px;
        padding: 2rem;
        background-color: rgba(255, 255, 255, 0.8);
        border-radius: 0px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
        transition: margin-left 0.3s ease;
      }

      .success-message {
        text-align: center;
        padding: 2rem;
        margin-top: 130px;
        background-color: #4caf50;
        color: white;
        border-radius: 10px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      }

      .success-message h2 {
        margin-bottom: 1.5rem;
        font-size: 2.5rem;
      }

      .buttons {
        display: flex;
        justify-content: center;
        align-items: center;
        margin-top: 2rem;
        flex-wrap: wrap;
      }

      .buttons a {
        text-decoration: none;
        padding: 1.2rem 2.5rem;
        border-radius: 8px;
        color: #fff;
        cursor: pointer;
        transition: background-color 0.3s ease;
        margin: 0.5rem;
        font-size: 1.1rem;
        background-color: #178733;
        border: 2px solid transparent;
        flex: 1;
        max-width: 200px;
        text-align: center;
      }

      .buttons a:hover {
        background-color: #147026;
      }

      @media screen and (max-width: 768px) {
        .menu-icon {
          display: block;
        }

        .sidebar {
          left: -60vw;
          width: 60vw;
        }

        .sidebar.open {
          left: 0;
        }

        main {
          margin-left: 0;
        }
      }

      /* Styles for the progress bar container */
      .progress-container {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 8px;
        background-color: #f0f0f0;
      }

      /* Styles for the progress bar */
      .progress-bar {
        height: 100%;
        background-color: green;
        width: 0;
        transition: width 0.2s ease-out;
      }
    </style>
  </head>
  <body>
    <div class="menu-icon" onclick="toggleSidebar()">
      <i class="fa fa-bars"></i>
    </div>

    <div class="sidebar" id="sidebar">
      <img class="logo" src="/images/logo.jpeg" alt="Logo" />
      <ul class="nav-links">
        <li>
          <a href="/landlord">
            <i class="fa fa-dashboard icon" style="color: #c68417"></i>
            Home
          </a>
        </li>
        <li>
          <a href="/landlord/add_hostel">
            <i class="fa fa-plus-square icon" style="color: #0da2f1"></i>
            Add Hostel
          </a>
        </li>
        <li>
          <a href="/landlord/delete_hostel">
            <i class="fa fa-trash-o icon" style="color: #eb0c0c"></i>
            Delete Hostel
          </a>
        </li>
        <li>
          <a href="/landlord/confirm_booking_landlord">
            <i class="fa fa-check-square icon" style="color: #048e04"></i>
            Approve Booking
          </a>
        </li>
        <li>
          <a href="/landlord/booking_history">
            <i class="fa fa-history icon" style="color: burlywood"></i>
            Bookings History
          </a>
        </li>
        <li>
          <a href="/landlord/landlord_profile">
            <i class="fa fa-user icon" style="color: gold"></i>
            My Profile
          </a>
        </li>
        <li>
          <a href="/logout">
            <i class="fa fa-sign-out icon" style="color: rgb(255, 0, 93)"></i>
            Sign-Out
          </a>
        </li>
      </ul>
    </div>

    <!-- Main content area -->
    <main>
      <div class="success-message">
        <h2>Hostel Added Successfully!</h2>
        <div class="buttons">
          <a href="/landlord/add_hostel">Add Another Hostel</a>
          <a
            href="/landlord"
            style="background-color: #0056b3; border-color: #0056b3"
            >Go Back to Homepage</a
          >
        </div>
      </div>
    </main>

    <script>
      function toggleSidebar() {
        var sidebar = document.getElementById("sidebar");
        sidebar.classList.toggle("open");
      }
    </script>
  </body>
</html>
